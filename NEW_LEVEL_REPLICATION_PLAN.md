# 新关卡学习复刻与制作方案

## 目标

在不破坏现有场景、事件和战斗系统的前提下，先学习已有地图和关卡的结构，再复刻一套新的可玩关卡。新关卡应尽量复用当前项目已有的：

- 地图图块、角色头像、动画和音效。
- 场景对象模型和战斗者模块。
- 公共事件、战斗 UI、奖励 UI 和存档逻辑。
- `GameBattle` 战斗流程和 `GameBattleHelper` 的范围/障碍计算。
- Game Creator 事件页和自定义命令。

这类工作主要发生在 Game Creator 编辑器和项目数据层，不需要先改 TypeScript。TypeScript 只在确实需要新增系统能力时才介入。

## 现有内容的参考关系

当前场景树已经提供了几种可以直接学习的关卡类型：

| 参考内容 | 适合学习的部分 | 复刻时的用途 |
| --- | --- | --- |
| 场景 `2`「开始」 | 开场事件、关卡选择 UI、公共事件调用 | 新关卡入口和关卡解锁 |
| 场景 `5`「关卡1-普通战役」 | 野外地图、宝箱、普通敌人和基础战斗 | 普通遭遇战模板 |
| 场景 `6`「关卡2-BOSS1」 | 哥布林阵容、Boss、玩家角色和战斗事件 | Boss 关模板 |
| 场景 `7`「关卡3-BOSS2」 | 多名敌人、Boss 和较复杂的战斗布局 | 高难度战斗模板 |
| 场景 `8`「关卡4-剧情战斗1」 | 当前对象数据中的 NPC、剧情阻挡、剧情点和城镇交互 | 剧情探索模板 |
| 场景 `13`「关卡4-剧情战斗2」 | 士兵、剧情角色、多单位战斗和剧情事件 | 剧情战斗模板 |
| 场景 `9` 至 `14` | 战斗背景、战斗地图和氛围资源 | 新战场的环境参考 |

推荐的学习顺序是：

```text
场景 2：知道关卡如何被打开
    -> 场景 5：知道普通战斗如何组织
    -> 场景 6/7：知道 Boss 战如何组织
    -> 场景 8：知道探索和剧情如何组织
    -> 场景 13：知道剧情战斗如何组织
```

## 推荐的新关卡方向

为了验证探索、事件和战斗的完整衔接，可以制作一个“边境驿站保卫战”关卡。这个主题不要求引入新的系统，能够使用现有的城镇/NPC/野外/战斗资源完成。

### 关卡概念

玩家队伍抵达边境驿站，先与驿站 NPC 交谈并接受任务，然后在战斗地图上抵挡敌人的两轮进攻，最后击败敌方首领。战斗结束后返回驿站，触发奖励和下一关解锁。

### 关卡目标

| 阶段 | 玩家目标 | 主要技术点 |
| --- | --- | --- |
| 探索开始 | 与任务 NPC 对话 | 场景对象点击事件、对话事件 |
| 战斗准备 | 进入战斗前确认队伍 | 战斗准备 UI、进入战斗事件 |
| 第一轮 | 击败普通敌人 | 基础阵营和行动逻辑 |
| 第二轮 | 敌人增援出现 | 战斗中生成战斗者或事件切换 |
| 首领战 | 击败敌方首领 | Boss 属性、状态、掉落和胜利条件 |
| 战斗结束 | 领取奖励并解锁后续内容 | 胜利开关、奖励事件、场景返回 |

### 推荐的最小可玩版本

第一版不要一开始就制作多分支剧情、复杂机关和多阶段 Boss，只保留以下闭环：

```text
进入地图
  -> 找到任务 NPC
  -> 接受任务
  -> 触发战斗
  -> 击败敌人
  -> 获得奖励
  -> 设置关卡完成开关
  -> 回到地图并显示完成结果
```

闭环稳定后，再加入增援、剧情分支、特殊目标和可选宝箱。

## 复刻工作流程

### 1. 建立参考样本

在编辑器中分别打开场景 `5`、`6`、`8` 和 `13`，为每个场景记录以下信息：

- 地图宽高和摄像机初始位置。
- 图层数量、图块类型和障碍层。
- 场景对象名称、模型、位置、朝向和显示层级。
- 哪些对象安装了战斗者模块 `6`。
- 哪些对象有点击、接触或自动执行事件。
- 场景事件页和状态页的用途。
- 使用了哪些公共事件和自定义命令。
- BGM、BGS 和战斗音效。
- 进入战斗前和战斗结束后的事件顺序。

建议先做一张“对象与事件清单”，不要直接凭画面复制。关卡真正的结构通常藏在场景对象事件、状态页和公共事件中。

### 2. 选择复制基底

根据新关卡类型选择最接近的基底：

- 普通野外关：以场景 `5` 为基底。
- 单 Boss 战：以场景 `6` 或 `7` 为基底。
- 城镇剧情关：以场景 `8` 为基底。
- 剧情战斗关：以场景 `13` 为基底。

复制时应在 Game Creator 编辑器中使用场景复制或另存为功能，并给新场景分配新的场景 ID。不要直接把旧场景文件覆盖成新内容，也不要手工修改加密的 `GameJam.gamecreator` 文件。

### 3. 先复制结构，再替换内容

第一轮复刻只保留原关卡的结构：

- 保留地图图层和障碍设置。
- 保留玩家进入点。
- 保留战斗入口事件。
- 保留战斗准备和战斗结束事件。
- 保留奖励、胜利开关和返回流程。
- 删除不需要的敌人、宝箱和装饰对象。

确认复制后的关卡可以正常进入、触发战斗、结束战斗和返回后，再开始换地图、换角色和换剧情。这样出现问题时能够快速判断是复制流程问题还是新内容问题。

### 4. 修改地图层

地图修改顺序建议如下：

1. 先确定玩家出生点和战斗区域。
2. 再确定主路径、绕路路径和不可通行区域。
3. 设置固定障碍、图块障碍、桥属性和动态障碍。
4. 放置掩体、墙体、道路、装饰和可交互区域。
5. 最后调整地图边缘、摄像机范围和背景音乐。

项目地图格子为 `48 x 48`。战斗者、宝箱、NPC 和剧情点应尽量放在格子中心，避免视觉位置和战斗网格位置不一致。

地图设计时要特别检查：

- 玩家出生点不能被障碍或场景对象堵住。
- 战斗者之间不能出现初始重叠。
- 玩家到任务 NPC 之间至少有一条明确可达路径。
- 敌人的出生格不能超出地图边界。
- 关键道路不能被装饰对象错误地变成不可通行。
- 桥、浅水、悬崖等特殊地形的障碍层和桥属性要一致。

## 场景对象设计

### 玩家对象

玩家对象应沿用项目已有的玩家场景对象结构，不要复制成普通敌人对象。需要确认：

- 玩家出生位置。
- 玩家对象模型和头像。
- 移动速度、朝向和阴影。
- 玩家场景对象的事件页。
- 场景切换时是否保存位置。

### NPC 和剧情对象

NPC 至少需要明确以下内容：


| 内容 | 设计要求 |
| --- | --- |
| 名称 | 名称应能反映对象用途，例如“驿站长”或“受伤士兵” |
| 位置 | 放在玩家容易发现、但不堵塞主路径的位置 |
| 朝向 | 默认朝向玩家或符合场景构图 |
| 触发方式 | 点击、接触、自动触发或仅作背景 |
| 事件页 | 对话、任务接受、任务完成和重复交互 |
| 开关条件 | 使用独立开关或玩家开关控制状态变化 |
| 状态页 | 任务前、任务中、任务完成可使用不同状态页 |

剧情阻挡和剧情点应优先复用场景 `8` 中的对象结构。剧情阻挡的任务是控制玩家路线，不应同时承担复杂对话；对话应放在附近的 NPC 或剧情点上。

### 战斗者

战斗者对象需要安装 `SoModule_Battler`，即模块 ID `6`，并确认：

- 角色数据 ID 正确。
- 阵营为玩家阵营或敌方阵营。
- 是否必须参加战斗。
- 初始朝向和位置。
- 是否允许玩家控制。
- 角色等级、技能、装备和状态。
- 死亡后是否保留、消失或触发事件。
- 金币、经验、道具和装备掉落。

不要仅仅替换战斗者的头像。战斗数据真正取决于绑定的角色数据、技能、装备、状态和阵营设置。

## 战斗关卡设计

### 战斗区域

一个新的战斗场景至少要定义：

- 玩家队伍的部署区域。
- 敌方初始部署区域。
- 可移动区域。
- 不可通行区域。
- 关键目标位置。
- 敌人增援位置。
- 摄像机能够覆盖的范围。

推荐先用简单的矩形或走廊结构验证战斗，再增加高低差、桥、狭窄通道和绕后路线。

### 敌人配置

第一版敌人数量建议控制在玩家队伍数量的 `1` 到 `1.5` 倍。敌人应至少分成两类：

- 近战单位：主动接近玩家并制造前排压力。
- 远程或技能单位：迫使玩家考虑站位和行动顺序。

Boss 不应只拥有更高的 HP。更好的做法是让 Boss 具备至少一种行为差异：

- 攻击范围更大。
- 有特殊状态或反击能力。
- 会召唤增援。
- 受到特定状态影响时改变行为。
- HP 低于某个比例后进入第二阶段。

第二阶段可以先通过战斗事件和状态页实现，不必马上修改战斗核心代码。

### 战斗事件

项目已有战斗自定义命令可以支持以下流程：

- `customCommand_9001`：开始战斗。
- `customCommand_9002`：结束战斗并设置胜负结果。
- `customCommand_9004` / `9005`：增减战斗者 HP / SP。
- `customCommand_9006`：添加或移除状态。
- `customCommand_9007`：显示伤害或治疗效果。
- `customCommand_9008`：生成新的战斗者。

新关卡应尽量使用这些已有能力，流程示例：

```text
进入战斗前事件
  -> 设置战斗参数
  -> 显示战斗准备界面
  -> 开始战斗
  -> 玩家和 AI 轮流行动
  -> 判断普通敌人是否全部死亡
  -> 必要时生成增援
  -> 判断 Boss 是否死亡
  -> 结束战斗
  -> 结算奖励
  -> 执行战斗后事件
```

战斗结束事件应明确区分：

- 胜利。
- 失败。
- 玩家主动中断。
- 事件或存档恢复后的继续执行。

不要把胜利逻辑只放在 UI 按钮事件中。应通过战斗结果开关和战斗结束公共事件统一处理，避免 AI 或事件直接结束战斗时漏掉奖励。

## 关卡流程设计

推荐将新关卡拆成四类事件：

### 入场事件

负责：

- 显示开场文本或立绘。
- 播放地图 BGM/BGS。
- 设置玩家出生位置。
- 初始化本关变量和开关。
- 触发 NPC 或敌人的初始状态。

### 探索事件

负责：

- NPC 对话。
- 任务接受。
- 宝箱和可交互物。
- 剧情阻挡和路线解锁。
- 进入战斗前的确认。

### 战斗事件

负责：

- 进入战斗前演出。
- 战斗者初始化。
- 增援、Boss 阶段和特殊条件。
- 胜利/失败/中断判断。
- 奖励和经验结算。

### 结束事件

负责：

- 关闭战斗 UI。
- 恢复地图和玩家状态。
- 设置关卡完成开关。
- 解锁下一关。
- 显示奖励或结果文本。
- 保存或返回关卡选择界面。

## 数据和开关规划

新关卡制作前先建立一张数据表，避免使用含义不明的变量。

| 数据 | 示例含义 |
| --- | --- |
| 关卡完成开关 | `关卡5已完成` |
| 任务接受开关 | `驿站任务已接受` |
| 第一轮完成开关 | `第一轮敌人已击败` |
| Boss 阶段开关 | `Boss已进入第二阶段` |
| 战斗结果开关 | 当前战斗胜利/失败 |
| 增援计数变量 | 已生成的增援批次 |
| 奖励领取开关 | 防止重复领取奖励 |

关卡事件建议使用“前置条件 + 事件执行 + 结果开关”的形式：

```text
前置条件：任务未接受
  -> 显示任务对话
  -> 设置任务接受开关

前置条件：任务已接受且战斗未完成
  -> 显示战斗提示
  -> 开始战斗

前置条件：战斗已完成且奖励未领取
  -> 显示完成对话
  -> 发放奖励
  -> 设置奖励领取开关
```

这样可以避免玩家重复对话时重新开始战斗或重复领取奖励。

## UI 和资源复用

新关卡应优先复用现有 UI，不新增界面类型：

- 战斗准备界面。
- 战斗菜单、技能、道具和目标选择界面。
- 战斗者状态窗口。
- 经验和击杀奖励界面。
- 对话和提示界面。
- 关卡选择界面。

表现层可以通过现有自定义命令完成：

- 显示/移动图片。
- 显示动画。
- 显示立绘。
- 播放 BGM、BGS 和音效。
- 显示伤害和治疗数字。
- 播放战斗特效。

资源替换时应先检查现有资源路径和尺寸，避免因为图片比例、头像帧数或动画帧率不一致造成显示问题。第一版关卡不建议同时引入大量新图片、视频和新字体。

## 平衡建议

新关卡可以先按“可完成”进行配置，再进行难度调整。

### 第一版平衡基线

- 玩家队伍全员至少有一个可用行动。
- 玩家出生点不应立即被敌人包围。
- 普通敌人可以在 `1` 到 `2` 个回合内接触玩家前排。
- 远程敌人不应从出生点直接覆盖整个玩家部署区。
- Boss 的单次攻击不应直接击败满 HP 的核心角色。
- 玩家至少有一条不依赖随机结果的通关路线。
- 奖励应和本关消耗、战斗长度以及失败风险相匹配。

### 需要观察的数据

- 平均战斗回合数。
- 玩家角色死亡数量。
- 技能使用频率和 SP 消耗。
- 道具使用数量。
- 玩家是否经常无法到达目标格。
- 敌人 AI 是否反复停在障碍前。
- Boss 是否被玩家绕过或卡住。
- 失败后重新进入关卡是否能正确恢复状态。

## 测试顺序

不要一次把所有内容都放完再测试。推荐分阶段验证。

### A. 地图测试

- 能否进入新场景。
- 玩家出生点是否正确。
- 地图边界是否正确。
- 障碍、桥和可通行区域是否符合设计。
- NPC、宝箱和剧情点是否可到达。

### B. 事件测试

- NPC 第一次交互是否触发任务。
- 重复交互是否进入正确状态页。
- 任务开关是否正确写入。
- 战斗入口是否只触发一次。
- 战斗结束后是否返回原事件。

### C. 战斗测试

- 玩家队伍是否正确进入战斗。
- 敌方阵营是否正确识别。
- 移动范围和攻击范围是否正确。
- 技能、道具、状态和反击是否正常。
- 敌人死亡是否触发奖励累计。
- 增援是否出现在可用格子。
- Boss 死亡是否能结束战斗。

### D. 存档测试

- 战斗前能否保存。
- 战斗中读档后战斗状态是否恢复。
- 场景对象的开关和状态是否恢复。
- 战斗失败后能否重新进入。
- 关卡完成后读档是否不会重复发奖励。

### E. 多输入测试

- 键盘能否完成主要流程。
- 鼠标能否选择场景对象和 UI。
- 手柄焦点和确认/返回是否正常。
- 移动端或不同窗口尺寸下 UI 是否溢出。

## 常见风险

| 风险 | 原因 | 处理方式 |
| --- | --- | --- |
| 复制关卡后改坏原关卡 | 直接覆盖原场景或复用同一 ID | 使用另存为并分配新场景 ID |
| NPC 无法触发 | 触发类型、距离或状态页条件不匹配 | 对照场景 `8` 检查对象事件和触发条件 |
| 战斗者不行动 | 缺少模块 `6`、阵营或角色数据错误 | 检查 `SoModule_Battler` 和角色数据 |
| 战斗结束没有奖励 | 胜利事件没有统一调用奖励流程 | 复用场景 `6/7/13` 的战斗结束结构 |
| 增援卡在墙里 | 生成格不可通行或已有对象占用 | 预先设计增援格并测试障碍数据 |
| 玩家重复领奖 | 只显示结果，没有领取开关 | 增加“奖励已领取”开关 |
| 读档后事件重复 | 事件执行状态没有被保存或恢复 | 使用项目现有存档数据和场景自定义数据机制 |
| UI 不能用手柄操作 | 新增控件没有接入焦点管理 | 复用现有 UI 和 `FocusButtonsManager` |
| 修改后运行内容没变化 | 只改了 TypeScript，没有更新 `out/` | 修改后执行项目构建，再检查 `Game.html` 加载的脚本 |

## 交付标准

新关卡达到以下条件后，才算完成第一版：

- 有独立的新场景 ID，不覆盖旧场景。
- 有明确的玩家出生点和可通行主路径。
- 至少一个 NPC 或剧情入口。
- 至少一场可完成的战斗。
- 胜利和失败分支都能执行。
- 奖励只发放一次。
- 关卡完成状态能够保存和恢复。
- 键盘、鼠标和手柄至少完成主要操作。
- 新关卡不会改变既有关卡的通关结果。
- 没有为了实现单个关卡而修改战斗核心逻辑，除非确认现有事件系统无法满足需求。

## 推荐实施顺序

```text
1. 记录场景 5、6、8、13 的结构
2. 选择一个最接近的场景作为复制基底
3. 在编辑器中复制并分配新场景 ID
4. 保留原事件结构，先验证进入和退出流程
5. 替换地图图层和场景对象
6. 配置 NPC、剧情点、战斗者和敌人阵营
7. 调整战斗入口、增援、Boss 和奖励事件
8. 设置关卡开关、解锁条件和防重复领取逻辑
9. 按地图、事件、战斗、存档、输入顺序测试
10. 最后再调整美术、音效和难度
```

本方案只描述新关卡的学习和制作方法。本轮不修改现有 TypeScript、JSON、资源、编译产物或工程数据。
